material {
	name : "Built-in fallback lit material",
	shadingModel : lit,
	blending : opaque
}

fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);

        material.baseColor = vec4(vec3(0.8), 1.0);
	}
}


